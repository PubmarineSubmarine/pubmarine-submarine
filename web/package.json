{
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "dependencies": {
    "preact": "^10.29.0",
    "@preact/signals": "^1.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
